[package]
name = "openloomi"
version = "0.8.9"
description = "OpenRice AI Assistant - Your Personal Productivity Tool"
authors = ["OpenRice Team"]
edition = "2021"
autobins = false
default-run = "openrice"

[lib]
name = "openloomi_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "openrice"
path = "src/main.rs"

[[bin]]
name = "openloomi-ctl"
path = "src/bin/openloomi.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "=2.11.0", features = ["devtools", "tray-icon", "macos-private-api", "protocol-asset"] }
tauri-plugin-shell = "2"
tauri-plugin-opener = "2"
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
tauri-plugin-window-state = "2"
tauri-plugin-store = "2"
tauri-plugin-notification = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-single-instance = "2"
device_query = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
log = "0.4"
env_logger = "0.10"
futures-util = "0.3"
url = "2.5"
reqwest = { version = "0.12", features = ["blocking", "json", "rustls-tls"] }
base64 = "0.22"
sha2 = "0.10"
time = { version = "=0.3.36", features = ["formatting"] }
sys-locale = "0.3"
image = "0.25"
# Filesystem watcher — `pet::config_watcher` listens for external
# edits of `~/.openloomi/pet-config.json` and the user's custom
# theme directory under `~/.openloomi/pet-custom/`.
notify = "6"
# System audio capture: cross-platform loopback/output capture.
# - macOS 14.6+: cpal loopback (cpal 0.17+)
# - Windows: WASAPI loopback via cpal
# - Linux: PulseAudio/PipeWire monitor via cpal
cpal = "0.18"

[target.'cfg(target_os = "macos")'.dependencies]
block2 = "0.6"
core-foundation = "0.10"
# `catch-all` wraps every `msg_send!` in a `@catch` and converts the
# Objective-C `@throw` into a Rust panic, which `panic_guard::catch_unwind_str`
# (used in `pet/macos_window.rs`) can then actually catch. Without this the
# ObjC exception unwinds straight into `tao::did_finish_launching` and
# Rust aborts with "Rust cannot catch foreign exceptions" — see
# https://github.com/openloomi/openloomi/issues/314 cold-launch abort.
objc2 = { version = "0.6", features = ["catch-all"] }
objc2-av-foundation = { version = "0.3.2", features = ["AVCaptureDevice", "AVMediaFormat", "block2"] }
objc2-foundation = { version = "0.3.2", default-features = false, features = ["NSString", "NSProcessInfo", "alloc"] }
# Updated to 7.x: Swift concurrency issues fixed in newer versions
# TEMPORARILY DISABLED: screencapturekit = { version = "7", features = ["macos_13_0"] }
# Screen capture: xcap uses ScreenCaptureKit on macOS (no PipeWire/libspa dependency)
xcap = "=0.9.4"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
